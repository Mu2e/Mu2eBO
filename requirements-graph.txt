# Dependencies for the LangGraph runner (.venv-graph rebuild recipe — see
# wiki/incidents/venv-relocated-to-data-volume.md).
# Install into a venv: python3 -m venv .venv-graph && source .venv-graph/bin/activate
#                      pip install -r requirements-graph.txt
#
# (langgraph-cli/grandalf/streamlit/plotly/pandas removed 2026-07-17 with the
# dead Studio/Streamlit dev overlay — nothing else imported them.)
langgraph>=0.2.50
langgraph-checkpoint-sqlite>=1.0.0
# skopt + scipy are imported via autoresearch_bo_michael.py.
scikit-optimize>=0.10
scipy>=1.10
numpy>=1.24
# ROOT file reader for bo-prodtarget harvest-pot-only (pot_vd.root TTrees + genCountLogger TH1D).
uproot>=5
awkward>=2
