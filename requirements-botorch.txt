# Rebuild recipe for .venv-botorch — the PRODUCTION picker venv (default of
# graph/config.py BOTORCH_VENV_PY; closed_loop hard-fails without it).
# Versions recorded 2026-07-17 from the live venv's dist-infos (the venv had
# no requirements file before — it was unreproducible if /data was lost; see
# wiki/incidents/venv-relocated-to-data-volume.md).
#
# Python 3.9 (pins the py3.9 floor on modes.py / autoresearch_bo_michael.py,
# which botorch_predict.py imports in-process).
#
# NOTE: the live venv carries 5.8 GB of CUDA wheels by accident; the picker
# is CPU-only (botorch_predict.py pins DEVICE=cpu). A rebuild should use CPU
# wheels: pip install --index-url https://download.pytorch.org/whl/cpu torch
# first, then the rest.
#
# matplotlib matters: this is the ONLY project interpreter with matplotlib —
# the off-repo cloud/saturation renderers run on it.
botorch==0.10.0
gpytorch==1.11
torch==2.8.0
numpy==2.0.2
scipy==1.13.1
matplotlib==3.9.4
