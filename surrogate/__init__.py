"""The MCP surrogate door onto surrokit: adapter.py (AutoresearchAdapter --
problems/history/suggest over modes.STUDIES, the production pick path) and
mcp_server.py (make_server over it, on stdio). Plain-Python clients import
core/botorch_predict.py directly (build_problem, compute_explore_picks).
"""
