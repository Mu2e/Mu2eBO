# Rebuild recipe for .venv-botorch-new — the botorch 0.18 A/B arm (selected
# via AUTORESEARCH_BOTORCH_VENV=.venv-botorch-new; see graph/config.py).
# Versions recorded 2026-07-17 from the live venv's dist-infos.
#
# Python 3.11, uv-built WITHOUT pip (audit installed packages via
# site-packages/*.dist-info, not `pip list`). CPU torch wheel (+cpu local
# version) — build with:
#   uv venv --python 3.11 <dir> && uv pip install --python <dir>/bin/python \
#     --index-url https://download.pytorch.org/whl/cpu torch==2.13.0+cpu
#   uv pip install --python <dir>/bin/python -r requirements-botorch-new.txt
#
# No matplotlib here (deliberate — renderers stay on .venv-botorch).
botorch==0.18.1
gpytorch==1.15.2
numpy==2.4.6
scipy==1.17.1
