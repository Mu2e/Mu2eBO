# =============================================================================
# Runtime state (LangGraph, Python, build, etc.)
# =============================================================================
graph_data/
.venv*
.claude/
build/
__pycache__/
*.pyc
.env

# =============================================================================
# Per-iteration BO workspaces (rendered proposals, preflight scratch)
# Consolidated 2026-07-17 into bo_work/{proposals,preflight}/<mode>/.
# =============================================================================
bo_work/
bo_*_preflight/
bo_*_proposals/

# =============================================================================
# Driver runtime logs and scratch
# =============================================================================
loop_logs/
gdml_dumps/
*.log
*.root
*.art

# =============================================================================
# LaTeX build intermediates (docs/slides/ beamer deck; moved there 2026-07-17)
docs/slides/*.aux
docs/slides/*.nav
docs/slides/*.out
docs/slides/*.snm
docs/slides/*.toc
docs/slides/*.vrb
# =============================================================================
slides/*.aux
slides/*.nav
slides/*.out
slides/*.snm
slides/*.toc
slides/*.vrb

# =============================================================================
# Editor / OS cruft
# =============================================================================
*~
#*#
.#*

# =============================================================================
# Leaderboard / pending backups (originals are committed)
# =============================================================================
*.bak
*.bak_*
*.bak.*
*.bak-*
*.broken.tsv

# =============================================================================
# Runtime locks (cross-process file locks; anchors live in locks/)
locks/
# =============================================================================
*.lock

# =============================================================================
# CVMFS / external sub-repos (vendored with their own .git)
# =============================================================================
backing
runs
Run1BAna/

# =============================================================================
# Pending-BO in-flight queue (mutates on every iteration; not a durable record)
# =============================================================================
pending_bo_*.tsv
/data/

# =============================================================================
# LaTeX build artifacts (beamer decks in docs/; .tex and .pdf are tracked)
# =============================================================================
docs/*.aux
docs/*.nav
docs/*.out
docs/*.snm
docs/*.toc

# subagent-driven-development scratch workspace
.superpowers/
