# =============================================================================
# Runtime state (LangGraph, Python, build, etc.)
# =============================================================================
graph_data/
.venv-*
.claude/
build/
__pycache__/
*.pyc
.env

# =============================================================================
# Per-iteration BO workspaces (rendered proposals, preflight scratch)
# =============================================================================
bo_*_preflight/
bo_*_proposals/

# =============================================================================
# Driver runtime logs and scratch
# =============================================================================
loop_logs/
gdml_dumps/
*.log
*.root
*.art

# =============================================================================
# LaTeX build intermediates (slides/ beamer deck)
# =============================================================================
slides/*.aux
slides/*.nav
slides/*.out
slides/*.snm
slides/*.toc
slides/*.vrb

# =============================================================================
# Editor / OS cruft
# =============================================================================
*~
#*#
.#*

# =============================================================================
# Leaderboard / pending backups (originals are committed)
# =============================================================================
*.bak
*.bak_*
*.bak.*
*.bak-*
*.broken.tsv

# =============================================================================
# Runtime locks (cross-process file locks)
# =============================================================================
*.lock

# =============================================================================
# CVMFS / external sub-repos (vendored with their own .git)
# =============================================================================
backing
runs
Run1BAna/
aitools/

# =============================================================================
# Pending-BO in-flight queue (mutates on every iteration; not a durable record)
# =============================================================================
pending_bo_*.tsv
/data/
